---
const { tags, classes = "" } = Astro.props;
---

<div class="flex flex-wrap gap-2">
  {
    tags.map((tag) => (
      <div class:list={[classes, "px-2 py-1 rounded border-2"]}>{tag}</div>
    ))
  }
</div>
