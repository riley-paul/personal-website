---
const { tags, classes = "" } = Astro.props;
---

<div class="flex flex-wrap gap-2">
  {
    tags.map((tag) => (
      <div
        class:list={[classes, "px-2 py-1 rounded bg-gray-100 text-slate-900"]}
      >
        {tag}
      </div>
    ))
  }
</div>
