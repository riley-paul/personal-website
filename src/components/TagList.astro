---
const { tags } = Astro.props;
---

<div class="flex gap-2 py-2 flex-wrap">
  {
    tags.map((tag) => (
      <div class="text-xs px-2 py-0 rounded-full bg-neutral-100 text-neutral-500 shadow">
        {tag}
      </div>
    ))
  }
</div>