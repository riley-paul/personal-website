---
import Layout from "../../layouts/Layout.astro";
import getModels from "../../utils/getModels";
const models = await getModels();
---

<Layout>
  {
    models.map((model) => (
      <div>
        <a href={`3D_models/${model}`}>{model}</a>
      </div>
    ))
  }
</Layout>
