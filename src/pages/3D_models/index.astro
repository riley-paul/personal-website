---
import Layout from "../../layouts/Layout.astro";
import getModels from "../../utils/getModels";
const models = await getModels();
---

<Layout>
  {models.map((model) => <a href={`3D_models/${model}`}>{model}</a>)}
</Layout>
